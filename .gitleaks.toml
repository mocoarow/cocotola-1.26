[extend]
# useDefault will extend the base configuration with the default gitleaks config:
# https://github.com/gitleaks/gitleaks/blob/master/config/gitleaks.toml
useDefault = true

[allowlist]
paths = [
  '''local.yml''',
  '''go\.work\.sum''',
  '''go\.sum''',
  '''cocotola-init/seed/seeds/public_workbooks\.yaml''',
]
