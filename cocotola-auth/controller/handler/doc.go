// Package handler implements the HTTP handlers and routing for the cocotola-auth API.
// It exposes authentication endpoints (POST /authenticate, POST /logout, GET /me)
// via Gin, supporting both JSON body and cookie-based token delivery.
package handler
